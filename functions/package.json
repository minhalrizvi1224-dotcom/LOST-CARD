{
  "name": "lostcard-functions",
  "version": "1.0.0",
  "description": "LOST CARD — Firebase Cloud Functions (AI proxy)",
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.9.0"
  }
}
